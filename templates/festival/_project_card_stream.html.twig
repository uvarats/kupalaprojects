<turbo-stream action="update" target="project_frame_{{ project.id }}">
    <template>
        {{ include('festival/_project_card.html.twig', { 'project': project }) }}
    </template>
</turbo-stream>