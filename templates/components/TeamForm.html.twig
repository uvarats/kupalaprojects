<div {{ attributes }}>
    {{ form_start(form) }}
    <div class="mb-3">
        {{ form_row(form.name) }}
    </div>
    <div class="d-flex">
        <button type="submit" class="btn btn-primary w-100">Создать</button>
    </div>
    {{ form_end(form) }}
</div>