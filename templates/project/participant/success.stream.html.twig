<turbo-stream action="replace" target="registration-frame">
    <template>
        {{ include('project/participant/success-alert.html.twig', {
            'participant': participant
        }) }}
    </template>
</turbo-stream>